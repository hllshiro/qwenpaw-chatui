# i18n 国际化支持实施计划

> **致代理工作者：** 必须使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 技能逐任务实施此计划。步骤使用复选框（`- [ ]`）语法进行跟踪。

**目标：** 为 QwenPaw ChatUI 项目引入 i18n 国际化支持，使用 vue-i18n 库实现多语言支持。

**架构：** 使用 vue-i18n 库，翻译文件按模块分文件，支持按需加载。语言设置集成到现有设置系统，支持 URL 参数切换。

**技术栈：** Vue 3, vue-i18n, dayjs, Nuxt UI

---

## 文件结构

在定义任务之前，映射将创建或修改的文件：

### 创建的文件
- `src/locales/index.ts` — vue-i18n 配置和导入
- `src/locales/zh-CN/common.json` — 中文通用文本
- `src/locales/zh-CN/settings.json` — 中文设置文本
- `src/locales/zh-CN/chat.json` — 中文聊天文本
- `src/locales/zh-CN/components.json` — 中文组件文本
- `src/locales/en/common.json` — 英文通用文本
- `src/locales/en/settings.json` — 英文设置文本
- `src/locales/en/chat.json` — 英文聊天文本
- `src/locales/en/components.json` — 英文组件文本
- `src/composables/useI18n.ts` — i18n composable
- `src/utils/date.ts` — 日期本地化工具

### 修改的文件
- `package.json` — 添加 vue-i18n 和 dayjs 依赖
- `src/main.ts` — 集成 vue-i18n
- `src/App.vue` — 配置 Nuxt UI locale prop
- `src/composables/settings/definitions.ts` — 添加语言设置分组
- `src/router/index.ts` — 添加 URL 参数支持
- `src/pages/index.vue` — 国际化文本
- `src/pages/chat/[id].vue` — 国际化文本
- `src/components/Navbar.vue` — 国际化文本
- `src/components/SettingsModal.vue` — 国际化文本
- `src/components/SettingItem.vue` — 国际化文本
- `src/layouts/default.vue` — 国际化文本
- `docs/features.md` — 添加 i18n 功能
- `docs/architecture.md` — 添加 i18n 架构说明
- `AGENTS.md` — 更新国际化写法要求

## 任务分解

### 任务 1: 安装依赖

**文件：**
- 修改：`package.json`

- [x] **步骤 1: 安装 vue-i18n**

```bash
pnpm add vue-i18n@11
```

- [x] **步骤 2: 安装 dayjs**

```bash
pnpm add dayjs
```

- [x] **步骤 3: 验证依赖安装**

```bash
pnpm list vue-i18n dayjs
```

- [x] **步骤 4: 提交更改**

```bash
git add package.json pnpm-lock.yaml
git commit -m "deps: add vue-i18n and dayjs dependencies"
```

### 任务 2: 创建翻译文件结构

**文件：**
- 创建：`src/locales/zh-CN/common.json`
- 创建：`src/locales/zh-CN/settings.json`
- 创建：`src/locales/zh-CN/chat.json`
- 创建：`src/locales/zh-CN/components.json`
- 创建：`src/locales/en/common.json`
- 创建：`src/locales/en/settings.json`
- 创建：`src/locales/en/chat.json`
- 创建：`src/locales/en/components.json`

- [ ] **步骤 1: 创建中文通用文本**

```json
// src/locales/zh-CN/common.json
{
  "common": {
    "confirm": "确定",
    "cancel": "取消",
    "save": "保存",
    "delete": "删除",
    "rename": "重命名",
    "newSession": "新建会话",
    "settings": "设置",
    "loading": "加载中...",
    "error": "错误",
    "success": "成功",
    "copy": "复制",
    "copied": "已复制",
    "close": "关闭",
    "back": "返回",
    "next": "下一步",
    "previous": "上一步",
    "search": "搜索",
    "noResults": "无结果",
    "required": "必填",
    "optional": "可选"
  }
}
```

- [ ] **步骤 2: 创建中文设置文本**

```json
// src/locales/zh-CN/settings.json
{
  "settings": {
    "title": "设置",
    "general": {
      "label": "通用",
      "behavior": {
        "label": "行为",
        "expandReasoning": "默认展开推理摘要",
        "expandTools": "默认展开工具部分"
      },
      "notifications": {
        "label": "系统通知",
        "agent": "智能体",
        "agentDescription": "生成完成后发送通知",
        "permission": "权限",
        "permissionDescription": "当需要用户权限审批时发送通知",
        "error": "错误",
        "errorDescription": "当发生错误时发送通知"
      }
    },
    "shortcuts": {
      "label": "快捷键",
      "bindings": {
        "label": "快捷键绑定",
        "newChat": "新建会话",
        "search": "搜索会话",
        "openSettings": "打开设置"
      }
    },
    "appearance": {
      "label": "外观",
      "brand": {
        "label": "品牌",
        "name": "品牌名称",
        "icon": "品牌图标"
      },
      "theme": {
        "label": "主题",
        "colorScheme": "配色方案",
        "light": "浅色",
        "dark": "深色",
        "primaryColor": "主题颜色"
      },
      "typography": {
        "label": "字体",
        "sansFont": "界面字体",
        "monoFont": "代码字体"
      },
      "language": {
        "label": "语言",
        "locale": "界面语言",
        "zhCN": "简体中文",
        "en": "English"
      }
    },
    "advanced": {
      "label": "高级",
      "backup": {
        "label": "数据备份",
        "export": "配置导出",
        "exportDescription": "导出所有配置为 JSON 文件",
        "import": "配置导入",
        "importDescription": "从 JSON 文件导入配置"
      }
    },
    "developerMode": "已启用开发者模式",
    "developerModeDescription": "高级配置选项已显示",
    "exportSuccess": "导出成功",
    "exportSuccessDescription": "配置已下载",
    "exportError": "导出失败",
    "importSuccess": "导入成功",
    "importSuccessDescription": "已导入 {count} 项配置",
    "importError": "导入失败",
    "invalidConfig": "无效的配置文件"
  }
}
```

- [ ] **步骤 3: 创建中文聊天文本**

```json
// src/locales/zh-CN/chat.json
{
  "chat": {
    "welcome": "有什么可以帮你的？",
    "inputPlaceholder": "输入消息...",
    "send": "发送",
    "stop": "停止",
    "thinking": "思考中...",
    "thinkingProcess": "思考过程",
    "parameters": "参数",
    "result": "结果",
    "waitingApproval": "等待审批",
    "approved": "已批准",
    "denied": "已拒绝",
    "approve": "批准",
    "deny": "拒绝",
    "tool": "工具",
    "emptyState": "输入消息开始对话",
    "sessionNotFound": "会话未找到",
    "backToHome": "返回首页",
    "newSession": "新会话",
    "renameSession": "重命名会话",
    "deleteSession": "删除会话",
    "deleteConfirm": "确定要删除该会话吗？此操作不可撤销。",
    "inputNewName": "输入新名称",
    "edit": "编辑",
    "editing": "编辑中",
    "editCancel": "取消",
    "editSave": "保存",
    "sessionActions": "会话操作"
  }
}
```

- [ ] **步骤 4: 创建中文组件文本**

```json
// src/locales/zh-CN/components.json
{
  "components": {
    "navbar": {
      "newSession": "新建会话"
    },
    "settings": {
      "brandName": "QwenPaw"
    },
    "sidebar": {
      "today": "今天",
      "yesterday": "昨天",
      "lastWeek": "最近7天",
      "lastMonth": "最近30天",
      "older": "更早"
    }
  }
}
```

- [ ] **步骤 5: 创建英文翻译文件**

为每种英文翻译文件创建相同结构，翻译所有文本。

- [ ] **步骤 6: 提交更改**

```bash
git add src/locales/
git commit -m "feat(i18n): create translation files for zh-CN and en"
```

### 任务 3: 配置 vue-i18n

**文件：**
- 创建：`src/locales/index.ts`
- 修改：`src/main.ts`

- [ ] **步骤 1: 创建 vue-i18n 配置**

```typescript
// src/locales/index.ts
import { createI18n } from 'vue-i18n'
import zhCNCommon from './zh-CN/common.json'
import zhCNSettings from './zh-CN/settings.json'
import zhCNChat from './zh-CN/chat.json'
import zhCNComponents from './zh-CN/components.json'
import enCommon from './en/common.json'
import enSettings from './en/settings.json'
import enChat from './en/chat.json'
import enComponents from './en/components.json'

const i18n = createI18n({
  legacy: false,
  locale: 'zh-CN',
  availableLocales: ['zh-CN', 'en'],
  messages: {
    'zh-CN': {
      ...zhCNCommon,
      settings: zhCNSettings,
      chat: zhCNChat,
      components: zhCNComponents
    },
    en: {
      ...enCommon,
      settings: enSettings,
      chat: enChat,
      components: enComponents
    }
  }
})

export default i18n
```

- [ ] **步骤 2: 在 main.ts 中集成 vue-i18n**

```typescript
// src/main.ts
import './assets/css/main.css'

import { createApp } from 'vue'
import { createHead } from '@unhead/vue/client'
import ui from '@nuxt/ui/vue-plugin'
import i18n from './locales'

import App from './App.vue'
import router from './router'

const app = createApp(App)

const head = createHead()

app.use(head)
app.use(router)
app.use(ui)
app.use(i18n)

app.mount('#app')
```

- [ ] **步骤 3: 提交更改**

```bash
git add src/locales/index.ts src/main.ts
git commit -m "feat(i18n): configure vue-i18n and integrate with app"
```

### 任务 4: 配置 Nuxt UI locale prop

**文件：**
- 修改：`src/App.vue`

- [ ] **步骤 1: 更新 App.vue 配置 locale prop**

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import { useI18n } from 'vue-i18n'
import * as locales from '@nuxt/ui/locale'

const { locale } = useI18n()
</script>

<template>
  <UApp :locale="locales[locale]">
    <RouterView />
  </UApp>
</template>
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/App.vue
git commit -m "feat(i18n): configure Nuxt UI locale prop"
```

### 任务 5: 更新设置系统

**文件：**
- 修改：`src/composables/settings/definitions.ts`

- [ ] **步骤 1: 添加语言设置分组**

在 `src/composables/settings/definitions.ts` 中添加：

```typescript
// 在外观分类下新增语言分组
registerGroup({ key: 'language', label: '语言', category: 'appearance' })
```

- [ ] **步骤 2: 添加语言设置项**

```typescript
// 新增语言设置项
registerSetting({
  key: 'appearance.language.locale',
  label: '界面语言',
  type: 'select',
  defaultValue: 'zh-CN',
  category: 'appearance',
  group: 'language',
  advanced: true,
  options: [
    { label: '简体中文', value: 'zh-CN' },
    { label: 'English', value: 'en' }
  ]
})
```

- [ ] **步骤 3: 提交更改**

```bash
git add src/composables/settings/definitions.ts
git commit -m "feat(i18n): add language setting to appearance category"
```

### 任务 6: 创建 i18n composable

**文件：**
- 创建：`src/composables/useI18n.ts`

- [ ] **步骤 1: 创建 useI18n composable**

```typescript
// src/composables/useI18n.ts
import { useI18n as useVueI18n } from 'vue-i18n'
import { watch } from 'vue'
import { useSettings } from './settings'

export function useI18n() {
  const { locale } = useVueI18n()
  const { getValue, setValue } = useSettings()

  // 监听设置变化，同步更新语言
  watch(
    () => getValue('appearance.language.locale'),
    (newLocale) => {
      if (newLocale && newLocale !== locale.value) {
        locale.value = newLocale
      }
    },
    { immediate: true }
  )

  // 切换语言
  function setLocale(newLocale: string) {
    locale.value = newLocale
    setValue('appearance.language.locale', newLocale)
  }

  return {
    locale,
    setLocale
  }
}
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/composables/useI18n.ts
git commit -m "feat(i18n): create useI18n composable"
```

### 任务 7: 添加 URL 参数支持

**文件：**
- 修改：`src/router/index.ts`

- [ ] **步骤 1: 更新路由守卫添加语言参数支持**

```typescript
// src/router/index.ts
import type { RouteRecordRaw } from 'vue-router'
import { createRouter, createWebHistory } from 'vue-router'
import { routes, handleHotUpdate } from 'vue-router/auto-routes'
import { setupLayouts } from 'virtual:generated-layouts'
import { useSessions } from '../composables/useSessions'
import { useI18n } from '../composables/useI18n'

const router = createRouter({
  routes: setupLayouts(routes as RouteRecordRaw[]),
  history: createWebHistory()
})

router.beforeEach((to) => {
  // 语言参数
  if ('lang' in to.query) {
    const lang = to.query.lang as string
    if (['zh-CN', 'en'].includes(lang)) {
      const { setLocale } = useI18n()
      setLocale(lang)
    }
  }
  
  // 业务键参数
  if ('business_key' in to.query) {
    const businessKey = (to.query.business_key as string) || 'default'
    const { setBusinessKey } = useSessions()
    setBusinessKey(businessKey)
  }
})

if (import.meta.hot) {
  handleHotUpdate(router)
}

export default router
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/router/index.ts
git commit -m "feat(i18n): add URL parameter support for language switching"
```

### 任务 8: 创建日期本地化工具

**文件：**
- 创建：`src/utils/date.ts`

- [ ] **步骤 1: 创建日期本地化工具**

```typescript
// src/utils/date.ts
import dayjs from 'dayjs'
import 'dayjs/locale/zh-cn'
import 'dayjs/locale/en'

export function formatDate(date: Date | string | number, locale: string): string {
  return dayjs(date).locale(locale).format('YYYY-MM-DD')
}

export function formatDateTime(date: Date | string | number, locale: string): string {
  return dayjs(date).locale(locale).format('YYYY-MM-DD HH:mm')
}

export function formatRelativeTime(date: Date | string | number, locale: string): string {
  return dayjs(date).locale(locale).fromNow()
}
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/utils/date.ts
git commit -m "feat(i18n): create date localization utility"
```

### 任务 9: 迁移首页组件

**文件：**
- 修改：`src/pages/index.vue`

- [ ] **步骤 1: 更新首页使用国际化**

```vue
<!-- src/pages/index.vue -->
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useRouter } from 'vue-router'
import { useI18n } from 'vue-i18n'
import { useSessions } from '../composables/useSessions'
import { useSettings } from '../composables/settings'

const router = useRouter()
const { t } = useI18n()
const { createSession } = useSessions()
const { getValue } = useSettings()

const brandName = computed(() => getValue('appearance.brand.name') || 'QwenPaw')

const input = ref('')
const loading = ref(false)

async function onSubmit() {
  if (!input.value.trim()) return
  loading.value = true
  try {
    console.log('[Home] Creating session...')
    const session = await createSession()
    console.log('[Home] Session created:', session)
    const msg = input.value
    input.value = ''
    console.log('[Home] Redirecting to:', `/chat/${session.id}`)
    router.push({ path: `/chat/${session.id}`, query: { msg } })
  } catch (err) {
    console.error('[Home] Error:', err)
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <UDashboardPanel
    id="home"
    class="min-h-0"
    :ui="{ body: 'p-0 sm:p-0' }"
  >
    <template #header>
      <Navbar />
    </template>

    <template #body>
      <UContainer class="flex-1 flex flex-col justify-center gap-4 sm:gap-6 py-8">
        <h1 class="text-3xl sm:text-4xl text-highlighted font-bold">
          {{ brandName }}
        </h1>

        <p class="text-muted">
          {{ t('chat.welcome') }}
        </p>

        <UChatPrompt
          v-model="input"
          :status="loading ? 'streaming' : 'ready'"
          class="[view-transition-name:chat-prompt]"
          variant="subtle"
          :ui="{ base: 'px-1.5' }"
          @submit="onSubmit"
        >
          <template #footer>
            <UChatPromptSubmit
              color="neutral"
              size="sm"
            />
          </template>
        </UChatPrompt>
      </UContainer>
    </template>
  </UDashboardPanel>
</template>
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/pages/index.vue
git commit -m "feat(i18n): internationalize home page"
```

### 任务 10: 迁移聊天页组件

**文件：**
- 修改：`src/pages/chat/[id].vue`

- [ ] **步骤 1: 更新聊天页使用国际化**

```vue
<!-- src/pages/chat/[id].vue -->
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { $fetch } from 'ofetch'
import { useRoute } from 'vue-router'
import { useI18n } from 'vue-i18n'
import { useSessions } from '../../composables/useSessions'
import { useChat, type ChatMessage, type MessageBlock } from '../../composables/useChat'
import Navbar from '../../components/Navbar.vue'
import ChatComark from '../../components/chat/Comark'

const route = useRoute<'/chat/[id]'>()
const { t } = useI18n()
const { updateSession, sessions } = useSessions()

// ... 其他代码保持不变

const sessionName = computed(() => {
  const session = sessions.value.find(s => s.id === sessionId)
  return session?.name || t('chat.newSession')
})

// ... 其他代码保持不变
</script>

<template>
  <!-- ... 模板中使用 t() 函数替换所有硬编码文本 -->
  <div v-if="messages.length === 0 && status === 'ready'" class="flex items-center justify-center h-full text-muted text-sm">
    {{ t('chat.emptyState') }}
  </div>

  <!-- ... 其他模板内容 -->
</template>
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/pages/chat/[id].vue
git commit -m "feat(i18n): internationalize chat page"
```

### 任务 11: 迁移导航栏组件

**文件：**
- 修改：`src/components/Navbar.vue`

- [ ] **步骤 1: 更新导航栏使用国际化**

```vue
<!-- src/components/Navbar.vue -->
<script setup lang="ts">
import { useI18n } from 'vue-i18n'

const { t } = useI18n()
</script>

<template>
  <UDashboardNavbar
    class="absolute top-0 inset-x-0 border-b-0 z-10 backdrop-blur lg:backdrop-blur-none pointer-events-none sm:px-4"
    :ui="{ left: 'pointer-events-auto min-w-0', right: 'pointer-events-auto' }"
  >
    <template #left>
      <slot name="title" />
    </template>

    <template #right>
      <slot />

      <UColorModeButton />

      <UButton
        color="neutral"
        variant="ghost"
        icon="i-lucide-circle-plus"
        to="/"
        class="lg:hidden"
        :aria-label="t('components.navbar.newSession')"
      />
    </template>
  </UDashboardNavbar>
</template>
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/components/Navbar.vue
git commit -m "feat(i18n): internationalize navbar component"
```

### 任务 12: 迁移设置弹窗组件

**文件：**
- 修改：`src/components/SettingsModal.vue`

- [ ] **步骤 1: 更新设置弹窗使用国际化**

```vue
<!-- src/components/SettingsModal.vue -->
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useMediaQuery } from '@vueuse/core'
import { useI18n } from 'vue-i18n'
import { useSettings } from '../composables/settings'

const props = defineProps<{
  open: boolean
}>()

const emit = defineEmits<{
  'update:open': [value: boolean]
}>()

const { t } = useI18n()

const {
  getValue,
  setValue,
  exportSettings,
  importSettings,
  enableDeveloperMode,
  getVisibleCategories,
  getVisibleGroups,
  getVisibleSettings,
} = useSettings()

// ... 其他代码保持不变

function handleBrandClick() {
  brandClickCount.value++

  if (brandClickTimer) {
    clearTimeout(brandClickTimer)
  }

  brandClickTimer = setTimeout(() => {
    brandClickCount.value = 0
  }, 1000)

  if (brandClickCount.value >= 5) {
    brandClickCount.value = 0
    enableDeveloperMode()
    useToast().add({
      title: t('settings.developerMode'),
      description: t('settings.developerModeDescription'),
      color: 'success',
    })
  }
}

// ... 其他代码保持不变
</script>

<template>
  <UModal
    v-model:open="isOpen"
    class="w-[680px] max-w-[calc(100vw-2rem)] h-[520px] max-h-[calc(100vh-4rem)]"
    :ui="{ body: 'p-0 overflow-hidden' }"
  >
    <template #header>
      <div class="flex items-center gap-2">
        <UIcon name="i-lucide-settings" class="w-5 h-5" />
        <h2 class="text-lg font-semibold">{{ t('settings.title') }}</h2>
      </div>
    </template>

    <!-- ... 其他模板内容 -->
  </UModal>
</template>
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/components/SettingsModal.vue
git commit -m "feat(i18n): internationalize settings modal"
```

### 任务 13: 迁移布局组件

**文件：**
- 修改：`src/layouts/default.vue`

- [ ] **步骤 1: 更新布局使用国际化**

```vue
<!-- src/layouts/default.vue -->
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import type { DropdownMenuItem } from '@nuxt/ui'
import { useI18n } from 'vue-i18n'
import { useSessions } from '../composables/useSessions'
import { useSettings } from '../composables/settings'

const router = useRouter()
const route = useRoute()
const { t } = useI18n()
const { groupedSessions, fetchSessions, deleteSession, updateSession } = useSessions()
const { getValue } = useSettings()

// ... 其他代码保持不变

const items = computed(() => groupedSessions.value?.flatMap((group) => {
  return [{
    label: group[0],
    type: 'label' as const
  }, ...group[1].map(item => ({
    id: item.id,
    label: item.name || t('chat.newSession'),
    to: `/chat/${item.id}`,
    icon: 'i-lucide-message-circle',
    slot: 'chat' as const
  }))]
}))

function getChatActions(item: { id: string, label: string }): DropdownMenuItem[][] {
  return [[
    {
      label: t('common.rename'),
      icon: 'i-lucide-pencil',
      onSelect: () => startRename(item.id, item.label)
    }
  ], [
    {
      label: t('common.delete'),
      icon: 'i-lucide-trash',
      color: 'error' as const,
      onSelect: () => handleDelete(item.id)
    }
  ]]
}

// ... 其他代码保持不变
</script>

<template>
  <!-- ... 模板中使用 t() 函数替换所有硬编码文本 -->
  <UNavigationMenu
    :items="[{
      label: t('common.newSession'),
      to: '/',
      kbds: ['meta', 'o'],
      icon: 'i-lucide-circle-plus'
    }]"
    :collapsed="collapsed"
    orientation="vertical"
  >
    <!-- ... 其他模板内容 -->
  </UNavigationMenu>

  <!-- ... 其他模板内容 -->
</template>
```

- [ ] **步骤 2: 提交更改**

```bash
git add src/layouts/default.vue
git commit -m "feat(i18n): internationalize layout component"
```

### 任务 14: 更新文档

**文件：**
- 修改：`docs/features.md`
- 修改：`docs/architecture.md`
- 创建：`docs/modules/i18n.md`
- 修改：`AGENTS.md`

- [ ] **步骤 1: 更新 features.md**

在 `docs/features.md` 中添加：

```markdown
## 国际化支持

- [x] 多语言框架（支持中英文，可扩展）
- [x] 语言设置集成（外观 > 语言分组）
- [x] URL 参数支持（lang=?）
- [x] 100% 文本覆盖
- [x] 日期本地化（dayjs）
- [x] 语言切换无需刷新
```

- [ ] **步骤 2: 更新 architecture.md**

在 `docs/architecture.md` 中添加：

```markdown
### 5. 国际化模块

使用 vue-i18n 实现多语言支持，翻译文件按模块分文件。

**目录结构：**
```
src/locales/
├── zh-CN/          # 中文翻译
├── en/             # 英文翻译
└── index.ts        # vue-i18n 配置
```

**特点：**
- 按需加载翻译文件
- 支持 URL 参数切换语言
- 与 Nuxt UI 深度集成
- 日期本地化支持

详细说明请参考 [国际化模块文档](./modules/i18n.md)。
```

- [ ] **步骤 3: 创建 i18n 模块文档**

```markdown
# 国际化模块

## 概述

国际化模块为 QwenPaw ChatUI 提供多语言支持，使用 vue-i18n 库实现。

## 职责

1. 管理翻译文件
2. 提供语言切换功能
3. 集成 Nuxt UI 的 locale prop
4. 支持 URL 参数切换语言

## 相关文档

- [架构设计文档](../architecture.md)
- [功能清单](../features.md)
- [国际化设计文档](../superpowers/specs/2026-05-25-i18n-support-design.md)

## 使用方法

### 在组件中使用翻译

```vue
<script setup lang="ts">
import { useI18n } from 'vue-i18n'

const { t } = useI18n()
</script>

<template>
  <button>{{ t('chat.send') }}</button>
</template>
```

### 切换语言

```typescript
import { useI18n } from '../composables/useI18n'

const { setLocale } = useI18n()
setLocale('en') // 切换到英文
```

### URL 参数切换

```
http://localhost:3000/?lang=en
```

## 翻译文件结构

```
src/locales/
├── zh-CN/
│   ├── common.json      # 通用文本
│   ├── settings.json    # 设置文本
│   ├── chat.json        # 聊天文本
│   └── components.json  # 组件文本
├── en/
│   ├── common.json
│   ├── settings.json
│   ├── chat.json
│   └── components.json
└── index.ts
```

## 添加新语言

1. 在 `src/locales/` 下创建新语言目录（如 `ja/`）
2. 复制现有翻译文件并翻译
3. 在 `src/locales/index.ts` 中导入新语言
4. 在 `src/composables/settings/definitions.ts` 中添加语言选项
```

- [ ] **步骤 4: 更新 AGENTS.md**

在 AGENTS.md 中添加：

```markdown
## 国际化支持

所有界面实现时必须使用国际化支持的写法：

### 必须使用 $t() 函数

```vue
<!-- 正确 -->
<button>{{ $t('chat.send') }}</button>

<!-- 错误 -->
<button>发送</button>
```

### 日期格式化使用 dayjs

```typescript
import { formatDate } from '../utils/date'
import { useI18n } from 'vue-i18n'

const { locale } = useI18n()
const formatted = formatDate(new Date(), locale.value)
```

### 翻译键命名规范

- 通用文本：`common.xxx`
- 设置文本：`settings.xxx`
- 聊天文本：`chat.xxx`
- 组件文本：`components.xxx`
```

- [ ] **步骤 5: 提交更改**

```bash
git add docs/features.md docs/architecture.md docs/modules/i18n.md AGENTS.md
git commit -m "docs: add i18n documentation and update conventions"
```

### 任务 15: 测试和验证

**文件：**
- 无（测试任务）

- [ ] **步骤 1: 测试语言切换功能**

1. 启动开发服务器：`pnpm dev`
2. 打开设置 > 外观 > 语言
3. 切换语言，验证界面文本立即更新
4. 刷新页面，验证语言设置已保存

- [ ] **步骤 2: 测试 URL 参数支持**

1. 访问 `http://localhost:3000/?lang=en`
2. 验证界面显示英文
3. 访问 `http://localhost:3000/?lang=zh-CN`
4. 验证界面显示中文

- [ ] **步骤 3: 验证所有文本已国际化**

1. 检查所有组件是否使用 `$t()` 函数
2. 检查是否有遗漏的硬编码文本
3. 验证日期格式化是否正常工作

- [ ] **步骤 4: 运行 lint 和 typecheck**

```bash
pnpm lint
pnpm typecheck
```

- [ ] **步骤 5: 提交最终更改**

```bash
git add .
git commit -m "feat(i18n): complete i18n implementation"
```

## 执行说明

1. **任务顺序**：按任务编号顺序执行
2. **每个任务独立**：可以单独执行，不影响其他任务
3. **提交信息规范**：使用 `feat(i18n):` 前缀表示 i18n 功能变更
4. **测试要求**：每个任务完成后应验证功能正常

## 验证方法

1. 测试语言切换功能
2. 验证所有文本已国际化
3. 测试 URL 参数支持
4. 测试日期格式化
5. 运行 lint 和 typecheck
6. 检查文档更新